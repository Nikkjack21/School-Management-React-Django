import { createSlice } from "@reduxjs/toolkit";


const INITIAL_STATE={
     formData:[],
}


const handleSubmit = createSlice({
    name: 'form',
    initialState:INITIAL_STATE,
    reducers:{
        
    }
    
})